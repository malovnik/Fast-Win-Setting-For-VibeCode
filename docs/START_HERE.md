# 🚀 НАЧНИ ОТСЮДА

## ✅ Что уже готово

Я создал для тебя полную среду разработки с оптимизацией под Python, Node.js, React и Rust.

### 📁 Созданные файлы:

```
Dev/
├── setup-dev-env.ps1         ← Установка всех инструментов
├── check-setup.ps1            ← Проверка установки
├── init-project.ps1           ← Инициализация проекта
├── QUICKSTART.md              ← Быстрый старт (ЧИТАЙ ПЕРВЫМ!)
├── README.md                  ← Полная документация
├── CHEATSHEET.md              ← Шпаргалка команд
├── pyproject.toml             ← Python конфигурация
├── package.json               ← Node.js конфигурация
├── .npmrc                     ← pnpm настройки (экономия места!)
├── .editorconfig              ← Единый стиль кода
├── .gitignore                 ← Git игнорирование
├── .prettierrc                ← Prettier конфигурация
├── eslint.config.js           ← ESLint конфигурация
└── .vscode/                   ← VSCode настройки
    ├── settings.json          ← Настройки редактора
    ├── extensions.json        ← Рекомендуемые расширения
    ├── launch.json            ← Конфигурации отладки
    └── tasks.json             ← Задачи сборки
```

## 🎯 ЧТО ДЕЛАТЬ СЕЙЧАС

### 1️⃣ Установи окружение (10-15 минут)

Открой **PowerShell от администратора**:

```powershell
cd C:\Users\malovnik\Documents\Dev
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
.\setup-dev-env.ps1
```

### 2️⃣ Проверь установку

```powershell
# Перезапусти PowerShell, затем:
.\check-setup.ps1
```

### 3️⃣ Читай документацию

```powershell
# Быстрый старт (5 минут чтения)
cat QUICKSTART.md

# Или в VSCode:
code QUICKSTART.md
```

## 💡 Ключевые фишки настройки

### ✨ Экономия места (~70%)
- **pnpm** вместо npm - общее хранилище пакетов
- Все `node_modules` - это симлинки
- Больше не нужно копировать гигабайты

### 🔧 Изоляция без конфликтов
- Python: виртуальные окружения `.venv`
- Node: pnpm с изолированными зависимостями
- Rust: отдельный cargo home

### 🎨 Единый стиль кода
- EditorConfig для всех языков
- Prettier для JS/TS/React/HTML/CSS
- Black для Python
- rustfmt для Rust

### 📦 Всё в одном месте
```
C:\DevTools\
├── Python\      ← Все версии Python
├── Node\        ← Все версии Node + pnpm
└── Rust\        ← Rust toolchains
```

### 🚫 Система остается чистой
- Никаких глобальных пакетов в системе
- Легко удалить всё - просто удали `C:\DevTools`
- Переменные окружения указывают на DevTools

## 📚 Дальнейшее чтение

1. **QUICKSTART.md** - Начни с этого! Пошаговая инструкция
2. **README.md** - Полная документация с примерами
3. **CHEATSHEET.md** - Шпаргалка по всем командам

## 🆘 Помощь

### Частые проблемы

**PowerShell не дает запустить скрипт?**
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**Команда не найдена после установки?**
```powershell
# Перезапусти PowerShell или обнови PATH:
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

**Python/Node/Rust не установились?**
```powershell
# Проверь что установлено:
.\check-setup.ps1

# Установи вручную через scoop:
scoop install pyenv
scoop install nvm
scoop install rustup
```

## 🎓 Примеры использования

### Python проект
```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
poetry install
python src/main.py
```

### React проект
```powershell
pnpm create vite my-app --template react-ts
cd my-app
pnpm install
pnpm dev
```

### Rust проект
```powershell
cargo new my-project
cd my-project
cargo run
```

---

## ⚡ Быстрые команды

```powershell
# Установка
.\setup-dev-env.ps1

# Проверка
.\check-setup.ps1

# Инициализация
.\init-project.ps1

# Читать доки
cat QUICKSTART.md
cat README.md
cat CHEATSHEET.md
```

---

**Удачи в разработке! 🚀**
