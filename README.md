# 🚀 Среда разработки для Python, Node.js, React и Rust

Оптимизированная среда разработки для Windows с экономией места и изоляцией проектов.

## 📋 Быстрый старт

### 1. Установка (автоматическая)

Открой **PowerShell от администратора** и выполни:

```powershell
cd path\to\this\folder
.\scripts\complete-setup.ps1
```

Это установит всё необходимое за 10-15 минут.

### 2. Проверка установки

```powershell
.\scripts\verify-install.ps1
```

## 📁 Структура проекта

```
Dev/
├── scripts/          # Скрипты установки и настройки
│   ├── complete-setup.ps1      # Полная автоматическая установка
│   ├── setup-simple.ps1        # Установка базовых инструментов
│   ├── check-setup.ps1         # Проверка установки
│   ├── verify-install.ps1      # Проверка и финализация
│   ├── init-project.ps1        # Инициализация нового проекта
│   └── init-shared.ps1         # Создание shared окружений ⭐
│
├── docs/             # Документация
│   ├── START_HERE.md           # Начни отсюда!
│   ├── QUICKSTART.md           # Быстрый старт с примерами
│   ├── README.md               # Полная документация
│   └── CHEATSHEET.md           # Шпаргалка команд
│
├── .shared/          # Общие окружения (экономия места!) ⭐
│   ├── venv/                   # Общее Python виртуальное окружение
│   ├── node_modules/           # Общие Node.js модули
│   └── README.md               # Инструкции по использованию
│
├── projects/         # Workspace проекты (pnpm workspaces) ⭐
│
├── .vscode/          # Настройки VSCode
├── pyproject.toml    # Конфигурация Python (Poetry)
├── package.json      # Конфигурация Node.js + workspaces
├── .npmrc           # Настройки pnpm
├── .editorconfig    # Единый стиль кода
└── .gitignore       # Git игнорирование
```

## 🎯 Что будет установлено

### Языки и рантаймы:
- **Python 3.14.0** - последняя версия Python
- **Node.js 24.11.1 LTS** - стабильная версия Node
- **Rust 1.91.1** - системный язык программирования

### Менеджеры версий:
- **pyenv-win** - управление версиями Python
- **nvm-windows** - управление версиями Node.js
- **rustup** - управление Rust toolchains

### Менеджеры пакетов:
- **pip** + **pipx** - Python пакеты
- **Poetry** - современный менеджер зависимостей Python
- **pnpm** - экономный менеджер Node пакетов (~70% экономии места!)
- **cargo** - Rust package manager

### Python утилиты:
- **black** - автоматическое форматирование кода
- **ruff** - быстрый линтер
- **mypy** - проверка типов
- **pytest** - тестирование

### Node.js утилиты:
- **TypeScript** - типизация для JavaScript
- **tsx** - выполнение TypeScript
- **ESLint** - линтер для JS/TS
- **Prettier** - форматирование кода

## 💡 Ключевые преимущества

### 1. Экономия места (~70%)
pnpm использует общее хранилище пакетов в `C:\DevTools\Node\pnpm-store`. Вместо копирования пакетов в каждый проект, создаются символические ссылки.

**До:**
```
project1/node_modules/ - 500 MB
project2/node_modules/ - 500 MB
project3/node_modules/ - 500 MB
ИТОГО: 1.5 GB
```

**После:**
```
C:\DevTools\Node\pnpm-store/ - 500 MB
project1/node_modules/ - ссылки (~5 MB)
project2/node_modules/ - ссылки (~5 MB)
project3/node_modules/ - ссылки (~5 MB)
ИТОГО: ~515 MB (экономия 66%)
```

### 2. Изоляция проектов
Каждый проект использует:
- Свою версию Python (через pyenv)
- Свою версию Node.js (через nvm)
- Своё виртуальное окружение (`.venv` для Python)
- Свои изолированные зависимости

### 3. Легкое переключение версий
```powershell
# Python
pyenv install 3.11.0
pyenv local 3.11.0

# Node.js
nvm install 20.0.0
nvm use 20.0.0

# Rust
rustup install nightly
rustup default nightly
```

### 4. Чистая система
Все окружения находятся в `C:\DevTools\`. Для полной очистки:
```powershell
Remove-Item -Recurse -Force C:\DevTools
```

## 📚 Документация

### Для начала работы:
1. **[docs/START_HERE.md](docs/START_HERE.md)** - начни с этого файла
2. **[docs/QUICKSTART.md](docs/QUICKSTART.md)** - пошаговые инструкции
3. **[docs/CHEATSHEET.md](docs/CHEATSHEET.md)** - шпаргалка команд

### Полная документация:
- **[docs/README.md](docs/README.md)** - подробное руководство

## 🎓 Примеры использования

### Python проект

```powershell
# Создать виртуальное окружение
python -m venv .venv

# Активировать
.venv\Scripts\Activate.ps1

# Установить зависимости через Poetry
poetry init
poetry add requests
poetry install

# Или через pip
pip install -r requirements.txt

# Запустить код
python main.py
```

### React проект (Vite)

```powershell
# Создать React приложение
pnpm create vite my-app --template react-ts

# Перейти в проект
cd my-app

# Установить зависимости
pnpm install

# Запустить dev сервер
pnpm dev

# Собрать для production
pnpm build
```

### Rust проект

```powershell
# Создать новый проект
cargo new my-project

# Перейти в проект
cd my-project

# Запустить
cargo run

# Собрать release версию
cargo build --release

# Тесты
cargo test
```

## 🔧 Скрипты

### Основные:
- `scripts\complete-setup.ps1` - Полная автоматическая установка
- `scripts\verify-install.ps1` - Проверка и финализация установки
- `scripts\check-setup.ps1` - Проверка текущего состояния

### Дополнительные инструменты:
- `scripts\install-all-extras.ps1` - ⭐ Установить ВСЁ дополнительное (Docker, БД, и т.д.)
- `scripts\install-docker.ps1` - Docker Desktop
- `scripts\install-databases.ps1` - PostgreSQL, MongoDB, Redis + GUI
- `scripts\install-api-tools.ps1` - Postman, Insomnia
- `scripts\install-python-extras.ps1` - Jupyter, Django, FastAPI, Data Science
- `scripts\install-frontend-tools.ps1` - Next.js, Tailwind, shadcn/ui
- `scripts\install-terminal-tools.ps1` - Oh My Posh, fzf, bat, ripgrep
- `scripts\install-linters.ps1` - Markdown, YAML, JSON linters
- `scripts\install-monitoring-tools.ps1` - Process Explorer, btop, Wireshark

### Проекты:
- `scripts\init-project.ps1` - Инициализация нового проекта
- `scripts\init-shared.ps1` - Создание shared окружений
- `scripts\setup-simple.ps1` - Базовая установка инструментов

## 🆘 Решение проблем

### PowerShell не запускает скрипты
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Команды не найдены после установки
Перезапусти PowerShell или обнови PATH:
```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

### Python/Node/Rust не работают
Проверь установку:
```powershell
.\scripts\check-setup.ps1
```

### pnpm ошибки с global bin
```powershell
pnpm setup --force
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверь [docs/README.md](docs/README.md) - там подробные инструкции
2. Загляни в [docs/CHEATSHEET.md](docs/CHEATSHEET.md) - шпаргалка по командам
3. Запусти `scripts\check-setup.ps1` для диагностики

## 🎯 Для преподавателей

Этот набор создан для обучения студентов разработке на современном стеке:
- Python - backend, data science, ML
- Node.js/React - frontend, full-stack
- Rust - системное программирование, WebAssembly

Преимущества для класса:
- Единая конфигурация для всех студентов
- Экономия места на компьютерах (важно для лабораторий)
- Изоляция проектов (студенты не сломают систему)
- Легко переустановить (один скрипт)

---

**Создано с ❤️ для обучения программированию**
