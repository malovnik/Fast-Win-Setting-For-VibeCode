# Быстрый старт

## Шаг 1: Установка окружения (один раз)

Открой PowerShell **от имени администратора** и выполни:

```powershell
# Перейди в директорию проекта
cd C:\Users\malovnik\Documents\Dev

# Разрешить выполнение скриптов
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

# Запустить установку
.\setup-dev-env.ps1
```

**Это займет 10-15 минут**. Скрипт установит:
- Scoop (менеджер пакетов)
- pyenv (управление версиями Python)
- nvm (управление версиями Node.js)
- rustup (управление Rust toolchains)
- pnpm (экономный менеджер пакетов для Node)
- Все необходимые утилиты

## Шаг 2: Проверка установки

```powershell
# Перезапусти PowerShell, затем:
.\check-setup.ps1
```

Должны увидеть зеленые галочки ✓ для всех инструментов.

## Шаг 3: Инициализация проекта

```powershell
# Инициализировать все окружения (Python + Node + Rust)
.\init-project.ps1

# Или только конкретное:
.\init-project.ps1 -ProjectType python
.\init-project.ps1 -ProjectType node
.\init-project.ps1 -ProjectType rust
```

## Шаг 4: Начало работы

### Python проект
```powershell
# Активировать виртуальное окружение
.venv\Scripts\Activate.ps1

# Установить зависимости
poetry install

# Запустить код
python src/main.py

# Или через poetry
poetry run python src/main.py
```

### Node.js / React проект
```powershell
# Установить зависимости
pnpm install

# Запустить dev сервер
pnpm dev

# Собрать проект
pnpm build
```

### Rust проект
```powershell
# Собрать
cargo build

# Запустить
cargo run

# Тесты
cargo test
```

## Структура проекта после настройки

```
C:\DevTools\                          # Все окружения здесь
├── Python\
│   ├── pyenv\                        # Версии Python
│   └── virtualenvs\                  # Виртуальные окружения
├── Node\
│   ├── nvm\                          # Версии Node.js
│   └── pnpm-store\                   # Общее хранилище пакетов
└── Rust\
    └── cargo\                        # Cargo зависимости

C:\Users\malovnik\Documents\Dev\      # Твой проект
├── .venv\                            # Python окружение для проекта
├── node_modules\                     # Node пакеты (симлинки в pnpm-store)
├── target\                           # Rust build артефакты
├── src\                              # Исходный код
├── tests\                            # Тесты
├── pyproject.toml                    # Python конфигурация
├── package.json                      # Node конфигурация
├── Cargo.toml                        # Rust конфигурация
└── .vscode\                          # VSCode настройки
```

## Ежедневная работа

### Переключение версий языков

```powershell
# Python
pyenv versions              # Список версий
pyenv install 3.11.0        # Установить версию
pyenv global 3.12.0         # Глобальная версия
pyenv local 3.11.0          # Локальная для проекта

# Node.js
nvm list                    # Список версий
nvm install 20.0.0          # Установить версию
nvm use 20.0.0              # Использовать версию

# Rust
rustup toolchain list       # Список toolchains
rustup default stable       # Использовать stable
```

### Работа с зависимостями

```powershell
# Python
poetry add requests                   # Добавить пакет
poetry add --group dev pytest         # Dev зависимость
poetry install                        # Установить все

# Node.js
pnpm add react                        # Добавить пакет
pnpm add -D typescript                # Dev зависимость
pnpm install                          # Установить все

# Rust
# Отредактируй Cargo.toml, затем:
cargo build                           # Автоматически установит
```

## VSCode

### Открыть проект
```powershell
code .
```

### Рекомендуемые расширения
При первом открытии VSCode предложит установить расширения из [.vscode/extensions.json](.vscode/extensions.json):
- Python
- Pylance
- Black Formatter
- Ruff
- ESLint
- Prettier
- Rust Analyzer
- И другие...

Нажми "Install All" для установки всех рекомендуемых расширений.

## Полезные файлы

- [README.md](README.md) - Полная документация
- [CHEATSHEET.md](CHEATSHEET.md) - Шпаргалка по командам
- [.editorconfig](.editorconfig) - Настройки форматирования
- [.gitignore](.gitignore) - Игнорируемые файлы

## Что дальше?

1. **Создай свой первый файл**:
   - Python: `src/main.py`
   - Node: `src/index.js` или `src/App.jsx`
   - Rust: уже создан `src/main.rs`

2. **Настрой под себя**:
   - Отредактируй `pyproject.toml` для Python
   - Отредактируй `package.json` для Node
   - Отредактируй `Cargo.toml` для Rust

3. **Начни писать код!**

## Решение проблем

### "Execution Policy" ошибка
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Python не находит модули
```powershell
# Активируй окружение
.venv\Scripts\Activate.ps1

# Переустанови зависимости
poetry install
```

### Node модули не найдены
```powershell
# Переустанови
rm -rf node_modules
pnpm install
```

### Rust linker ошибка
Установи Visual Studio Build Tools:
```powershell
scoop install vcredist2022
```

## Экономия места

Эта настройка экономит **много места**:

1. **pnpm** использует общее хранилище для Node пакетов
   - Вместо копирования, создает симлинки
   - Экономия ~70% места по сравнению с npm

2. **Виртуальные окружения** изолированы
   - `.venv` только для этого проекта
   - Не загрязняет систему

3. **Cargo** использует общий кэш
   - Зависимости загружаются один раз
   - Переиспользуются между проектами

## Следующие шаги

- Прочитай полную документацию: `cat README.md`
- Открой шпаргалку: `cat CHEATSHEET.md`
- Проверь настройку: `.\check-setup.ps1`
- Начни кодить! 🚀
